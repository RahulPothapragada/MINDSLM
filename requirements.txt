# MindSLM — Python Dependencies
# Install with: pip3 install -r requirements.txt

# API server
Flask==3.1.3
flask-cors==6.0.2
requests==2.33.1
python-dotenv==1.2.2

# ML / Classification
torch==2.11.0
transformers==5.6.1
sentence-transformers==5.4.1
scikit-learn==1.8.0
joblib==1.5.3
numpy==2.4.0

# RAG + Memory
chromadb==1.5.8

# Datasets (for rag.py --build)
datasets==4.8.4

# Fine-tuning (Kaggle only — not needed to run the API)
# peft==0.19.1
# accelerate==1.13.0
# bitsandbytes
# trl
